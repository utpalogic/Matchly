import 'package:flutter/material.dart';

/// These color themes would be used throughout the app.
class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF4CAF50);
  static const Color primaryDark = Color(0xFF388E3C);
  static const Color primaryLight = Color(0xFF81C784);

  // Background colors
  static const Color background = Color(0xFFF5F5F5);
  static const Color cardBackground = Colors.white;

  // Text colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFF9E9E9E);

  // Status colors
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFF44336);
  static const Color warning = Color(0xFFFF9800);
  static const Color info = Color(0xFF2196F3);

  // Booking status colors
  static const Color confirmed = Color(0xFF4CAF50);
  static const Color waitlisted = Color(0xFFFF9800);
  static const Color cancelled = Color(0xFFF44336);
  static const Color completed = Color(0xFF2196F3);
}
